<Window x:Class="DemoStylet.MainShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DemoStylet"
        mc:Ignorable="d"
        Title="MainShellView" Height="450" Width="800">
    <StackPanel Margin="10">
        <ProgressBar Value="{Binding ProgressValue}" Visibility="{Binding ProgressVisibility}" Maximum="100" Height="25" />
        <Slider Value="{Binding ProgressValue}" Maximum="100" Margin="0,10" />
        <CheckBox Content="Show" IsChecked="{Binding IsProgressShow}" />
    </StackPanel>
</Window>
